/**
 * Open access while the product is being mastered — no paywalls or checkout.
 * Set OPEN_ACCESS to false (or SP_DISABLE_LIMITS=false) when monetization returns.
 */
export const OPEN_ACCESS = true;
